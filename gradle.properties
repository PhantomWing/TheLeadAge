## Mod info
mod_version=1.1.1
mod_id=theleadage
mod_group_id=com.phantomwing.theleadage
mod_license=MIT License
mod_name=The Lead Age
mod_description=A vanilla-friendly expansion that adds lead, a new metal resource. A sister mod to The Silver Age.
mod_authors=PhantomWing

# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
org.gradle.jvmargs=-Xmx3G
# Architectury Loom requires the Gradle daemon itself to run on JDK 21. The system
# default `java` may be an older JDK, so the daemon JVM is pinned explicitly here.
# Adjust this path (or override in a non-committed gradle.properties /
# via -Dorg.gradle.java.home=) if your JDK 21 lives elsewhere.
org.gradle.java.home=C\:\\Program Files\\Eclipse Adoptium\\jdk-21.0.7.6-hotspot
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
# Architectury Loom is not yet compatible with the Gradle configuration cache.
org.gradle.configuration-cache=false

#read more on this at https://github.com/neoforged/ModDevGradle?tab=readme-ov-file#better-minecraft-parameter-names--javadoc-parchment
# you can also find the latest versions at: https://parchmentmc.org/docs/getting-started
parchment_minecraft_version=1.21.6
parchment_mappings_version=2025.06.29

# Minecraft 1.21.6
minecraft_version=1.21.6
minecraft_version_range=[1.21.6,1.21.7)
# Game versions tagged on the Modrinth/CurseForge uploads (space or comma separated). Kept separate
# from the build target so a release can advertise every version it actually runs on.
supported_minecraft_versions=1.21.6
neo_version=21.6.20-beta
neo_version_range=[21.6,)
loader_version_range=[4,)

# Architectury / multi-loader. 1.21.6 needs loom 1.14; Gradle wrapper stays 8.12.
# Versions match TheSilverAge branch version/1.21.6.
architectury_plugin_version=3.4.164
architectury_loom_version=1.14.476
architectury_version=17.0.6
enabled_platforms=fabric,neoforge

# Fabric
fabric_loader_version=0.16.14
fabric_api_version=0.128.2+1.21.6

# Cloth Config (Fabric only, REAL dep) — backs the Fabric AutoConfig config screen.
cloth_config_version=19.0.147

# Farmer's Delight Refabricated (Fabric only, COMPILE-ONLY). Never shipped and never in the dev
# runtime: it exists so the Lead Knife's superclass is the real KnifeItem at compile time, which
# makes the compiler check the shape that would otherwise only fail on a player's machine.
fdr_version=1.21.6-3.2.3

# Other dev-runtime dependencies (never shipped). AppleSkin: DROPPED on this line (no
# 1.21.4/1.21.5 build); re-add appleskin_*_version when one ships.

# --- 1.21.6 PORT NOTES ----------------------------------------------------------
# Availability verified against the JEI maven + Modrinth API for 1.21.6:
#   * JEI: SKIPS 1.21.6 entirely - the maven goes jei-1.21.5-* straight to jei-1.21.7-*
#     (jei-1.21.6-common-api is a 404). The recipe-viewer plugin is therefore DROPPED on
#     this line and returns on 1.21.7.
#   * FD Refabricated (Fabric): 1.21.6-3.2.3 EXISTS - the Lead Knife stays.
#   * Farmers Delight (NeoForge), Create, EMI: still no build - compat stays dropped.
#   * NeoForge 1.21.6 is a BETA (21.6.20-beta); matches TheSilverAge version/1.21.6.
# --------------------------------------------------------------------------------

# Publishing — Modrinth / CurseForge project IDs (PUBLIC identifiers, not secrets).
# Tokens come from the account-wide MODRINTH_TOKEN / CURSEFORGE_TOKEN env vars in CI, falling back
# to modrinth_token / curseforge_token in the global ~/.gradle/gradle.properties for local runs.
mod_publish_plugin_version=2.0.0
modrinth_project_id=zv6Yo2mC
curseforge_project_id=1544260
